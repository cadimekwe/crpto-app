import React from "react";

const Nav = (props) => {
  return (
    <div>
      <Nav>
        <link to="/"> Home</link>

        <link to="aboutUs">ABout Us</link>
      </Nav>
    </div>
  );
};

export default Nav;
